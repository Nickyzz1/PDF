package Prova;

public class Quest1 {
    private Integer valor1;
    //Atribuição por referência, é quando se usa uma classe padrão do Java, como por exemplo o  Integer.
    // Neste exemplo dizemos que a variável é do tipo Integer e que retorna não só o valor e sim o ponteiro da variável,
    // e diferente da atribuição por valor, quando ela está vazia não retorna 0 e sim null.

    private int valor2;
    //Atribuição por valor é quando a variável tem um tipo innt, por exemplo. Quando ela retorna apenas o valor da variável,
    // e não possui um tipo, como Integer.
    // Quando não é adicionado nenhum valor a ela, sempre irá retorna 0  
}
