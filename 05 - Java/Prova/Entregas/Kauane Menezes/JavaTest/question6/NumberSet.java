package question6;

import java.util.HashSet;

public class NumberSet extends Set {

    HashSet<Double> hash;

    @Override
    public boolean contains(Float num) {

        return (hash.contains(()num)) ? true : false;
        
    }
    
}
